server:
  port: 8084

spring:
  application:
    name: tracking-service
  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://ep-billowing-silence-b30jkujt-pooler.c-4.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_R7gOhQBkDUx4
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: tracking
        hbm2ddl:
          create_namespaces: true

  data:
    mongodb:
      uri: mongodb+srv://lananhhii121_db_user:lananh123@delivery-system.1fh7sey.mongodb.net/tracking_geo_db?retryWrites=true&w=majority&appName=Delivery-system
    redis:
      host: redis
      port: 6379

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: tracking-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

  flyway:
    enabled: false
    url: jdbc:postgresql://ep-billowing-silence-b30jkujt-pooler.c-4.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    user: neondb_owner
    password: npg_R7gOhQBkDUx4
    baseline-on-migrate: true
    clean-disabled: false
    clean-on-validation-error: true
    schemas: tracking
    locations: classpath:db/migration

openapi:
  service:
    title: Tracking Service API
    version: 1.0.0

logging:
  level:
    root: INFO
    web: INFO
    org.flywaydb: INFO

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html