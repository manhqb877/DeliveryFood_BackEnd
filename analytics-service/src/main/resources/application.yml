server:
  port: 8087

spring:
  application:
    name: analytics-service
  profiles:
    active: dev

  # Chỉ sử dụng MongoDB cho Analytics Service (analytics_db)
  data:
    mongodb:
      uri: mongodb+srv://lananhhii121_db_user:lananh123@delivery-system.1fh7sey.mongodb.net/analytics_db?retryWrites=true&w=majority&appName=Delivery-system

openapi:
  service:
    title: Analytics Service API
    version: 1.0.0

logging:
  level:
    root: INFO
    web: INFO

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html